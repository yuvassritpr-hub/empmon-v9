services:
  - type: web
    name: empmon-v9
    env: node
    buildCommand: npm install && npm run build
    startCommand: node server.js
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: empmon-db
          property: connectionString
      - key: NODE_ENV
        value: production
